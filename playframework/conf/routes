# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
POST     /createprofile             controllers.Application.crprofile()
GET     /createprofile             controllers.Application.crprofile()
GET     /patientprofile             controllers.Application.pprofile()
GET     /doctorprofile         	    controllers.Application.dprofile()
GET     /index             			controllers.Application.main()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
